{
  "name": "fetch-api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "build": "netlify deploy",
    "preview": "vite preview",
    "clean": "rm -rf node_modules dist"
  },
  "devDependencies": {
    "netlify-cli": "^12.0.11"
 
  } 
}
